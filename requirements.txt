# Transcription
faster-whisper>=1.1.0
yt-dlp>=2024.12.0

# Vector store & embeddings
chromadb>=0.6.0
sentence-transformers>=3.0.0
torch>=2.0.0          # required by sentence-transformers; install the CUDA build for GPU

# Chatbot UI
gradio>=4.44.0

# Claude API (optional — only needed if using Claude backend)
anthropic>=0.40.0

# Hybrid retrieval + reranking
rank-bm25>=0.2.2

# Numeric (semantic chunking + BM25 scoring)
numpy>=1.24.0

# Progress bars
tqdm>=4.65.0
